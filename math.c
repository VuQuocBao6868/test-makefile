#include "math.h"

// Function definitions
int add(int a, int b) {
    return a + b;
}

int minus(int a, int b) {
    return a - b;
}

long int multi(float a, float b) {
    return (long int)(a * b);
}

short division(float a, float b) {
    return (short)(a / b);
}
